# AI_AGENT_GUIDE

## وظيفة الوكيل

وكيل AI في هذا المشروع هو مساعد بحثي قابل للمراجعة. لا يملك صلاحية إعلان مسألة محلولة لمجرد أن استدلاله الداخلي يرجح ذلك.

## البروتوكول التشغيلي القياسي

`ERDOS_PROBLEM_SOLVING_PROTOCOL.md` هو البروتوكول التشغيلي القياسي للمسائل الرياضية في هذا المستودع وفق `DEC-013`.

قبل تفعيل أي مسألة، وأثناء الانتقال بين المراحل، يجب على الوكيل تحديد البوابة الحالية `G0`–`G10` وتطبيق شروط الدخول والخروج الملائمة. لا يجوز اختزال البروتوكول إلى checklist شكلية: كل انتقال يحتاج إلى الأدلة التي يطلبها مستوى الادعاء نفسه.

إذا تعارض البروتوكول العام مع قاعدة أشد في `CONSTITUTION.md` أو `DECISIONS.md` أو بروتوكول خاص بالمسألة، تُطبق القاعدة الأشد إلى أن يُحل التعارض صراحة. واعتماد البروتوكول لا يتجاوز بوابات إذن المستخدم، ومنها `DEC-012`.

## قبل البدء

### بوابة مزامنة الحالة

قبل أي بحث أو تعديل رياضي، يجب على الوكيل:

1. قراءة رأس المستودع والرسم الرئيسي للبناء.
2. التحقق من حالة CI الفعلية، لا الحالة المكتوبة في وثيقة قديمة.
3. مقارنة ذلك مع `PROJECT_STATE.md` والخريطة النشطة وملفات README.
4. إيقاف العمل الموضوعي وتحيين الوثائق إذا وجد تعارضاً مادياً.
5. عدم اعتبار أي ملف Lean متحققاً ما لم يكن مستورداً في الرسم الرئيسي وناجحاً في CI.

بعد اجتياز بوابة المزامنة، يجب على الوكيل:

1. تحديد رقم المسألة ومصدرها.
2. جلب النص الحالي والتحقق من تاريخ الحالة.
3. فحص أي formalization معروف.
4. البحث الأولي عن الأدبيات والنتائج السابقة.
5. تسجيل فرضياته قبل بناء برهان طويل عندما يكون ذلك مفيداً.

## بوابة دمج المستودع

بعد أن نسخ القرار `DEC-011` القرار `DEC-004`، يجب على الوكيل:

1. العمل في فرع مخصص وفتح طلب دمج نحو `main`.
2. تشغيل الفحص القانوني الذي يتضمن `lake exe mk_all --check` و`lake build`.
3. عدم دمج طلب أحمر أو ملف غير موجود في الرسم الذي يولده `mk_all`.
4. إضافة اختبارات حدود موجبة وسالبة عندما تتغير فرضيات مبرهنة حساسة.
5. مزامنة وثائق الحالة داخل طلب الدمج نفسه.
6. إبقاء `main` أخضر؛ غياب إعداد حماية إداري لا يجيز تجاوز هذه القاعدة.

## أثناء العمل

- يميز صراحة بين fact وinference وconjecture.
- لا يخفي الفجوات في البرهان.
- عندما يستخدم الحساب، يذكر حدود البحث وما يمكن وما لا يمكن استنتاجه.
- عندما يقترح theorem أو lemma، يوضح dependency graph عند الإمكان.
- لا يساوي بين حل variant وحل المسألة الأصلية.
- لا يكرر نتيجة من النموذج نفسه كأنها تحقق مستقل.
- يحافظ على التصنيف التشغيلي للفشل: statement/definition، mathematical counterexample، proof gap، project-owned، dependency-owned، package-resolution، infrastructure، artifact/provenance، أو post-build، بدلاً من اختزال كل شيء إلى "CI failed".

## بعد إنتاج نتيجة

يجب أن يحاول الوكيل مهاجمة نتيجته بنفسه عبر:

- البحث عن counterexamples صغيرة.
- اختبار الحالات الحدية.
- فحص quantifiers.
- فحص اتجاهات inequalities.
- البحث عن assumptions غير معلنة.
- محاولة بناء proof مستقل أو تجزئة مختلفة.
- البحث في الأدبيات عن النتيجة أو ما يشبهها.

وعندما تكون النتيجة قابلة للاختبار آلياً، تُحفظ الحالات السلبية المهمة كـregressions بدل حذفها بعد إصلاح المسار.

## معيار التوقف

إذا بقيت فجوة جوهرية، يجب أن يقول بوضوح إن البرهان غير مكتمل. لا يحول الرغبة في الإكمال إلى ادعاء اكتمال.

كما يجب التوقف عن تقدم المرحلة إذا تحقق أي stop condition ملزم في `ERDOS_PROBLEM_SOLVING_PROTOCOL.md`، ولا سيما غياب إذن التفعيل، غموض النص canonical، تعارض الحالة مع CI، falsification صريح للبناء، عدم وصول ملف Lean إلى الرسم القانوني، أو فشل exact-head verification.

## عندما تكون النتيجة واعدة

ينبغي تحويلها إلى `Research Record`، ثم إلى claims قابلة للمراجعة، ثم إلى formalization أو verification مناسب عندما يكون ذلك ممكناً.

## بروتوكول التعامل مع عدم اليقين

الأولوية:

`I don't know` > `unsupported guess`.

ويجب الفصل في اللغة بين:

- "أثبتنا"
- "نستنتج إذا صحت الفرضية X"
- "تشير الحسابات"
- "يبدو أن"
- "وجدنا في الأدبيات"
- "لم أجد بعد"

## استقلالية التحقق

لا يعتبر الوكيل مخرجه السابق دليلاً على نفسه. عندما تكون النتيجة حساسة، يجب طلب آلية تحقق خارجية: حساب مستقل، مراجع بشري، proof assistant، أو مصدر أدبي موثوق.
