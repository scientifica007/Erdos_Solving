# Erdős Problem #225 — Benchmark

> **CURRENT REPOSITORY STATUS — INACTIVE / PAUSED.** No candidate proof was produced, the reference solution was not accessed, and the blind boundary remains intact if this benchmark is explicitly resumed. The active project is #678.

## Problem-level classification

The main Erdős problem is classified as PROVED (LEAN) on erdosproblems.com. The status is verified at the problem level, not through a solved variant.

## Canonical statement

> Is there an entire non-linear function $f$ such that, for all $x\in\mathbb{R}$, $x$ is rational if and only if $f(x)$ is?

This statement is the benchmark target. No paraphrase is to replace it during the blind phase.

## Blind-mode restrictions

Until proof-freeze or explicit failure:

1. Do not inspect the historical solution.
2. Do not inspect solution discussions.
3. Do not inspect a formal proof.
4. External verification is limited to problem-level status and the canonical statement.

## Selection rationale

#246 was rejected as a benchmark because its currently published statement is literally false when $a=1$ or $b=1$. #225 is a cleaner fourth benchmark: one-line statement, problem-level proved status, and a direct construction is expected to be discoverable without specialized machinery.

## Objective

Construct an independent proof, audit it, freeze it if valid, and only then compare it with the reference solution.
